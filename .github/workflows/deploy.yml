name: Dev Deployment Pipeline

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  deploy:
    name: CI/CD Deploy to dev
    runs-on: [self-hosted, Linux]
    if: github.ref == 'refs/heads/dev'
    timeout-minutes: 30
    
    # Ensure we can use org-level runners
    permissions:
      contents: read
      actions: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0
      
      - name: Pull latest code
        run: |
          echo "Pulling latest code from repository..."
          git fetch origin
          git reset --hard origin/${{ github.ref_name }}
          git clean -fd
          echo "Current commit: $(git rev-parse HEAD)"
          echo "Current branch: $(git branch --show-current)"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Detect compose directory
        id: detect
        run: |
          if [ -f "./docker-compose.prod.yml" ]; then
            APP_PATH="."
            COMPOSE_FILE="docker-compose.prod.yml"
          elif [ -f "./docker-compose.yml" ]; then
            APP_PATH="."
            COMPOSE_FILE="docker-compose.yml"
          else
            APP_PATH="."
            COMPOSE_FILE="docker-compose.yml"
          fi
          echo "app_path=$APP_PATH" >> $GITHUB_OUTPUT
          echo "compose_file=$COMPOSE_FILE" >> $GITHUB_OUTPUT

      - name: Find .env file location
        id: env_file
        run: |
          echo "Searching for .env file..."
          # Check home directory first (where it's actually located)
          if [ -f "$HOME/Edify-Service-Chatbot-Agent/.env" ]; then
            ENV_FILE="$HOME/Edify-Service-Chatbot-Agent/.env"
            echo "Found .env at: $ENV_FILE"
          elif [ -f "/opt/edify-chatbot/.env" ]; then
            ENV_FILE="/opt/edify-chatbot/.env"
            echo "Found .env at: $ENV_FILE"
          elif [ -f "$HOME/edify-chatbot/.env" ]; then
            ENV_FILE="$HOME/edify-chatbot/.env"
            echo "Found .env at: $ENV_FILE"
          elif [ -f "${{ steps.detect.outputs.app_path }}/.env" ]; then
            ENV_FILE="${{ steps.detect.outputs.app_path }}/.env"
            echo "Found .env at: $ENV_FILE"
          else
            ENV_FILE="${{ steps.detect.outputs.app_path }}/.env"
            echo "⚠️  Warning: .env file not found, will use: $ENV_FILE"
            echo "Checked locations:"
            echo "  - $HOME/Edify-Service-Chatbot-Agent/.env"
            echo "  - /opt/edify-chatbot/.env"
            echo "  - $HOME/edify-chatbot/.env"
            echo "  - ${{ steps.detect.outputs.app_path }}/.env"
          fi
          echo "env_file=$ENV_FILE" >> $GITHUB_OUTPUT
          echo "✅ Using .env file at: $ENV_FILE"

      - name: Remove existing Docker container (if running)
        run: |
          CONTAINER_NAME=edify-chatbot
          if [ "$(docker ps -aq -f name=$CONTAINER_NAME)" ]; then
            echo "Removing existing container: $CONTAINER_NAME"
            docker rm -f $CONTAINER_NAME
          else
            echo "No existing container found: $CONTAINER_NAME"
          fi

      - name: Stop and remove containers with compose
        working-directory: ${{ steps.detect.outputs.app_path }}
        run: |
          docker compose \
            -f ${{ steps.detect.outputs.compose_file }} \
            --env-file ${{ steps.env_file.outputs.env_file }} \
            down --remove-orphans || true

      - name: Remove previous Docker images (if any)
        run: |
          IMAGE_NAME=edify-chatbot:latest
          if [ "$(docker images -q $IMAGE_NAME)" ]; then
            echo "Removing previous image: $IMAGE_NAME"
            docker rmi -f $IMAGE_NAME || true
          else
            echo "No existing image found: $IMAGE_NAME"
          fi

      - name: Clean old Docker resources
        run: |
          echo "Cleaning up old Docker resources..."
          docker container prune -f || true
          docker image prune -a -f || true
          docker volume prune -f || true
          docker builder prune -a -f || true
          echo "Cleanup completed"

      - name: Build latest Docker image with compose
        working-directory: ${{ steps.detect.outputs.app_path }}
        run: |
          echo "Building Docker image with compose..."
          docker compose \
            -f ${{ steps.detect.outputs.compose_file }} \
            --env-file ${{ steps.env_file.outputs.env_file }} \
            build --no-cache

      - name: Run Docker container with compose
        working-directory: ${{ steps.detect.outputs.app_path }}
        run: |
          echo "Starting containers with compose..."
          docker compose \
            -f ${{ steps.detect.outputs.compose_file }} \
            --env-file ${{ steps.env_file.outputs.env_file }} \
            up -d --force-recreate --remove-orphans
          echo "Deployment completed successfully"

      - name: Verify deployment
        run: |
          echo "Waiting for container to be healthy..."
          sleep 10
          if docker ps | grep -q edify-chatbot; then
            echo "✅ Container is running"
            docker ps | grep edify-chatbot
          else
            echo "❌ Container failed to start"
            docker logs edify-chatbot || true
            exit 1
          fi
          
      - name: Show container logs
        if: always()
        run: |
          echo "=== Container Logs ==="
          docker logs edify-chatbot --tail 50 || true
